# Project Handover Status

## Scope

MiMo Bridge MCP after P0-P6.26 implementation, TokenBudget real-event hookup, documentation cleanup, Windows 10/11 x64 packaging preparation, Reasonix TUI multi-agent parity slices, failed-task intervention UX, low-cost model routing/profile configuration, bounded completion wake/recovery, task attachments, and safe session terminal actions.

## Task Goal

Let Codex split and review work while MiMo performs bounded coding tasks through one shared localhost daemon, with low-token review and a user-friendly Windows launch/install path.

## Current Progress

- Branch: `master` (`master...origin/master [ahead 6]` before this documentation refresh).
- Current Ultra Speed feature note: MiMo backend task `task_104155a74501` was reviewed by Codex, manually absorbed into the main worktree, focused-tested, then its temporary Worktree was discarded and the task was marked accepted. Implemented backend support: `mimo-v2.5-pro-ultra-speed`, default-off `enable_mimo_pro_ultra_speed`, Ultra Speed pricing metadata, MiMo CLI model mapping, multimodal rejection, and stale-Ultra cleanup when the switch is explicitly turned off. Reasonix UI task `task_ce0745c6b01e` failed and was not continued; its Worktree was discarded and the task was marked abandoned. Use MiMo only for the remaining UI work in this slice. New reliability debts recorded in `docs/OPEN_TASKS.md`: safe-client JSON BOM handling, stronger completion/failure wakeup, Reasonix reliability audit, and observed empty-output safe-client calls where REST still works.
- Ultra Speed backend verification on 2026-06-26: `npm.cmd run build` passed, and `node --test tests\model-routing.test.mjs tests\mimo-runner.test.mjs tests\admin-api.test.mjs` passed 57/57 in both the MiMo task Worktree and the main worktree after absorption.
- Latest live verification after final P6.26 packaging: daemon health ok on `127.0.0.1:3210`, MCP status ready, MiMo and Reasonix configured, queue running/queued counts are 0, and `node scripts\mimo-review-wakeup.mjs --limit 5 --max-chars 8000` returned `pending_count: 0`.
- Latest local phase: P6.26 task-completion wake/recovery and MiMo model compatibility are implemented locally. Added `scripts/mimo-review-wakeup.mjs` as a low-context one-shot recovery command for both MiMo and Reasonix; it reports only pending task IDs, agent/status, risk flags, recommendation, and next review command. The Codex App heartbeat automation `mimo-bridge-review-wakeup` now runs this script every 5 minutes to bring the current thread back when tasks complete/fail after Codex stops waiting. Boundary: this is not a daemon-native hard push into Codex; it is a safe recovery inbox plus app heartbeat, so reviews remain bounded and Codex does not poll full task state. MiMo runner now maps UI model labels to CLI provider/model names (`mimo-v2.5-flash -> xiaomi/mimo-v2.5`, `mimo-v2.5-pro -> xiaomi/mimo-v2.5-pro`) and maps reasoning effort to `--variant`. This fixes the `ProviderModelNotFoundError` seen when MiMo CLI received raw `mimo-v2.5-flash`.
- P6.26 verification so far: `npm.cmd run build`, `node --test tests\mimo-runner.test.mjs tests\model-routing.test.mjs tests\agent-start-task.test.mjs`, and `node scripts\mimo-review-wakeup.mjs --limit 5 --max-chars 8000` passed. Historical pending tasks `task_8ee96c2c4189` and `task_dcd3b5a75a14` were reviewed via Review Package and are obsolete drafts because their useful changes were manually implemented and committed in later slices.
- P6.26 final packaging is complete. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,631,872 bytes) and portable ZIP `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,665,889 bytes, ignored). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `1fdeb68`.
- Latest local phase: P6.25 low-cost model routing/profile configuration is implemented locally. Admin UI now has a "模型路由" settings page and Create Task exposes task scenario, Auto/Manual routing, image/multimodal hint, Agent, model, reasoning effort, and Auto preview. Bridge exposes and persists `/api/routing-profiles`, validates MiMo/Reasonix model lists, forces multimodal to MiMo `mimo-v2.5-flash`, writes routing snapshots into task config/briefs/Review Package, passes MiMo `--model`, and maps Reasonix effort to `--max-steps` 10/20/40. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\model-routing.test.mjs tests\agent-start-task.test.mjs tests\admin-api.test.mjs tests\review-package.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-ui-task-actions.test.mjs` passed 74/74.
- P6.25 collaboration note: Reasonix task `task_c27e97cfd1d6` and MiMo task `task_158531fc17eb` were used as design/implementation sources but were not merged directly. Their useful changes have been manually absorbed into the main worktree; their old Worktrees were discarded and both tasks were marked abandoned. `recover` and `agent-recover --agent-id reasonix-tui` returned no pending tasks afterward.
- Latest local phase: P6.24 failed-task reply visibility and missed-intervention UX is implemented locally. Reasonix task `task_c27e97cfd1d6` failed because it hit `agent.max_steps` / exit code 1 after partial Admin UI edits; it is not out-of-scope and still has a valid Reasonix session file, so it is resumable. MiMo backend task `task_158531fc17eb` was interrupted before recording a MiMo session ID, so reply is impossible and it should be handled by focused review/discard. Admin API now augments task list/detail with `can_reply`, `reply_blockers`, and `reply_label`; Admin UI shows exact reply availability/reason, adds a top-bar/overview `待介入` count from `health.pending_reviews.count`, and uses generic "执行 Agent" wording instead of MiMo-only repair text. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\task-reply-capability.test.mjs tests\admin-api.test.mjs tests\pending-reviews.test.mjs tests\agent-reply-task.test.mjs tests\admin-ui-task-actions.test.mjs` passed 43/43.
- Operational note: failed/completed tasks are discoverable through recovery and the Admin UI count, but the daemon still cannot by itself wake the exact active Codex turn instantly. Keep using `recover` / `agent-recover` after interruptions; a stronger app-side wakeup/notification mechanism remains planned.
- Latest local phase: P6.23 failed-task recovery/continue is implemented locally. Reasonix UI-routing task `task_c27e97cfd1d6` failed because `reasonix run` hit `paused after 20 tool-call rounds (agent.max_steps)`, leaving partial Worktree edits and status `failed`. Previously, recovery only surfaced `review` tasks, so Codex could miss failed tasks needing intervention. Recovery now includes failed tasks with Worktrees/errors/issues/risk flags and terminal cancelled/abandoned tasks with residual Worktrees. Failed tasks can be replied to, and the Admin UI reply box is enabled for failed tasks so Reasonix can continue from its recorded session. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\pending-reviews.test.mjs tests\agent-reply-task.test.mjs tests\admin-ui-task-actions.test.mjs tests\mimo-bridge-client.test.mjs tests\admin-api.test.mjs` passed 68/68. Daemon was restarted; `health` is ready and `agent-recover --agent-id reasonix-tui` returns the failed task for Codex intervention.
- Caution: do not restart the local daemon while MiMo/Reasonix tasks are running unless necessary. The daemon restart interrupted MiMo backend-routing task `task_158531fc17eb`; it now appears as failed with a populated Worktree and should be handled through review/reply/discard using the low-context recovery flow.
- Latest local phase: P6.22 terminal Worktree cleanup is implemented and deployed locally. Failed, cancelled, and abandoned tasks with residual Worktrees now have an enabled "丢弃 Worktree 并放弃" path in the Admin UI. Backend discard remains strict for merge, but discard can clear terminal-task Worktree records even if the Worktree folder was already externally removed, after validating the stored root/task-id shape and pruning stale Git worktree metadata. Backend finish now permits terminal cleanup to `abandoned` but still rejects accepting failed tasks. Operation buttons are grouped into "审查结论", "清理 Worktree", and "队列与删除". Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\finish-task.test.mjs tests\merge-task.test.mjs tests\admin-ui-task-actions.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs` passed 42/42. Local daemon was restarted; `health` reports MCP ready on 127.0.0.1:3210. Active/running cancellation Worktree cleanup is still a separate audit item.
- Installer upgrade compatibility fix is implemented locally for the company-PC issue where the EXE stayed at 0 percent while text flashed. Root cause: an older installed daemon was still running from `%LOCALAPPDATA%\MiMoBridgeApp`, but `%LOCALAPPDATA%\MiMoBridge\launcher-state.json` was missing, so the launcher returned unmanaged/not-owned. The installer now has an upgrade-only fallback that stops the old daemon only when the port owner command line proves it is `node.exe` running `local-daemon` / `index.js` under the install root; otherwise it preserves the old install and asks the user to close MiMo Bridge or reboot.
- Release artifacts were refreshed after the installer upgrade compatibility fix. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,608,320 bytes) and local ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,642,150 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `f3d61e3`.
- Latest local phase: P6.21 Admin UI open actions are implemented locally. `/api/agent-tasks/:id/open` now supports fixed local-open actions for non-MiMo tasks after an `agent_id` guard; Reasonix task-folder/session-folder/GUI buttons route through the generic Agent route while MiMo keeps `/api/tasks/:id/open`. Browser clients still send only fixed actions, never arbitrary paths or commands. Verification passed: root build, local-daemon build, admin-ui build, `node --test tests\admin-ui-lifecycle-api.test.mjs`, and `node --test tests\admin-api.test.mjs tests\task-open-actions.test.mjs tests\agent-get-wait-task.test.mjs` passed 43/43.
- Latest local phase: P6.20 Admin UI task-detail reads are implemented locally. Detail refresh, focused file read, diff read, log tail, and full debug helpers now route non-MiMo tasks through `/api/agent-tasks/:id?...agent_id=...`; MiMo tasks keep `/api/tasks/:id?...`. Verification passed: admin-ui build, root build, and `node --test tests\admin-ui-task-detail-api.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs tests\agent-get-wait-task.test.mjs` passed 37/37.
- Release artifacts were refreshed after P6.20. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and local ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,925 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `692a8d8`.
- Latest local phase: P6.19 Admin UI Codex handoff is implemented locally. The "交给 Codex 审查" prompt is agent-aware: MiMo review prompts keep `mimo_get_task` / safe-client `review`, while Reasonix TUI prompts use `agent_get_task(agent_id="reasonix-tui", ...)` or `agent-review --agent-id reasonix-tui`. The reply box also uses the actual agent display name. Verification passed: admin-ui build, root build, and `node --test tests\codex-handoff.test.mjs tests\admin-ui-api.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs` passed 51/51.
- Release artifacts were refreshed after P6.19. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and local ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,860 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `d5749d3`.
- Latest local phase: P6.18 Admin UI Agent lifecycle parity is implemented locally. The UI now routes non-MiMo cancel/finish/worktree/delete operations through `/api/agent-tasks/:id/...` with `agent_id`, while MiMo tasks keep legacy `/api/tasks/:id/...` routes. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\admin-ui-lifecycle-api.test.mjs tests\agent-lifecycle-task.test.mjs tests\admin-api.test.mjs` passed 37/37.
- Release artifacts were refreshed after P6.18. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and local ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,641 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `78dffac`.
- Latest local phase: P6.17 Reasonix GUI companion opening is implemented locally. Reasonix task detail can request the daemon to open Reasonix GUI with the same configured `REASONIX_HOME`; the browser sends only fixed `action=reasonix_gui` and never sends local paths or commands. If a `reasonix-gui` agent is configured, its command is used; otherwise the daemon infers `...\ReasonixDesktop\reasonix-desktop.exe` from the Reasonix TUI config. Root build, local-daemon build, admin-ui build, and `node --test tests\task-open-actions.test.mjs tests\admin-api.test.mjs tests\agent-registry.test.mjs` passed 43/43.
- Boundary: P6.17 opens Reasonix GUI as a shared-session/manual companion only. It does not deep-link to a specific session because `reasonix --help` does not expose a stable GUI session-open command.
- Release artifacts were refreshed after P6.17. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,224 bytes) and local ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,531 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `1bafa35`.
- Latest local phase: P6.16 generic Review Package summary is implemented locally. Review Packages add `agent_summary` / `agent_summary_zh` while preserving legacy `mimo_summary` / `mimo_summary_zh`; the Admin UI prefers generic summary fields when present.
- P6.16 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\review-package.test.mjs tests\agent-start-task.test.mjs tests\admin-api.test.mjs` passed 51/51.
- P6.15 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\mimo-bridge-client.test.mjs tests\stdio-protocol.test.mjs tests\token-budget.test.mjs` passed 52/52. Daemon was restarted; live `health`, `agent-token-status`, and `agent-list` smoke passed. P6.14 token capability/UI copy remains implemented: ready Reasonix TUI reports `capabilities.token_usage=true`, and the Token page describes MiMo and Reasonix as contributors only when real logged token/cost data exists.
- Committed Bridge baseline before this handoff-doc refresh: `master...origin/master [ahead 1]`.
- Latest committed Bridge HEAD before this handoff-doc refresh: `01d734f docs: record session manager fallback collaboration`.
- This handoff-doc refresh may be uncommitted if the user did not ask for a Git commit; always run `git status --short --branch` after context compression.
- P0-P5.5 implementation is complete locally. P5.5 Dynamic Task Scope is merged at `0497211 merge: accept dynamic task scope`.
- Shared daemon, HTTP MCP, admin UI, task queue, Worktree review, safe deletion, safe-delete visibility, low-token review, and `mimo_wait_task` are implemented.
- Windows launcher lifecycle controls, persisted config, desktop shortcut command, opt-in autostart command, portable ZIP build, EXE installer build, installer self-test, and release-validation script are implemented.
- Local EXE install was repaired after a real double-click test exposed a broken installed launcher script. The installed app now starts from `%LOCALAPPDATA%\MiMoBridgeApp`, uses `%LOCALAPPDATA%\MiMoBridge` for data, and reports MCP ready at `http://127.0.0.1:3210/mcp`.
- Installer upgrade hardening is complete: unknown EXE flags are rejected instead of silently starting quiet install, `-Help` is supported, reinstall/update verifies the old daemon is stopped before replacing files, locked install files abort safely, replacement uses staging plus rollback, and `-SelfTest` now starts a temporary daemon and verifies `/api/health` plus Admin UI HTML without installing.
- Current release target is Windows 10/11 x64.
- Current generated release artifacts are ignored under `artifacts/`.
- P5.4/P6.10-P6.15 Safe Agent Invocation is implemented. Use `scripts/mimo-bridge-client.mjs` / `.ps1` for scripted bridge calls instead of ad hoc inline Node or PowerShell JSON. The client now supports both MiMo-specific commands and generic `agent-*` commands for Reasonix-safe delegation/review/reply/lifecycle/status actions.
- TokenBudgetManager now records real MiMo JSONL `part.tokens` / `part.cost` events and Reasonix session JSONL usage/cost fields when present. A zero value after daemon restart/reset means no completed task with explicit token data has been recorded in the current runtime.
- P5.5 Dynamic Task Scope is implemented. It keeps `allowedRoots` as the machine-level hard boundary and makes each task carry its own generated editable/read-only boundary snapshot. Repo-wide scope is allowed only with explicit confirmation. Reasonix must reuse this same mechanism in P6.
- Origin Codex thread handoff is implemented in the current worktree from MiMo task `task_9dd5e0aefba0`: tasks can carry `origin_codex_thread_id`, `origin_codex_thread_url`, and `origin_source`; the admin UI review handoff safely returns to the original Codex thread when available and falls back to a new thread otherwise.
- Pending review recovery inbox is implemented in the current worktree: `mimo_pending_reviews`, `GET /api/pending-reviews`, `health.pending_reviews.count`, and `scripts/mimo-bridge-client.mjs recover` let Codex recover completed MiMo tasks after interrupted waits without reading full logs, diff, source, or the full repo.
- P6.0/P6.1 first slice is implemented locally: Agent Registry, `agent_list`, `/api/agents`, health agent summary, config `agents[]`, and Reasonix TUI probe support. This only detects Reasonix; it does not yet allow Reasonix to execute or edit tasks.
- P6.2 runner/control/session slice is implemented locally: `agent_start_task`, `agent_get_task`, `agent_wait_task`, `/api/agent-tasks`, `/api/agent-tasks/:id`, `/api/agent-tasks/:id/wait`, and `ReasonixTuiRunner` can run and review one-shot Reasonix-style tasks through the existing Worktree, queue, dynamic scope, low-token wait, Review Package flow, and Reasonix session mapping. P6.3 first UI slice is also implemented: admin UI fetches `/api/agents`, adds an execution-agent selector, routes Reasonix creation through `/api/agent-tasks`, and displays agent badges.
- P6.4 Agent-aware queue/path-conflict scheduling is implemented locally: the global queue can run up to two tasks only when they are on different agents and their editable paths do not overlap. Same-agent tasks, overlapping paths, missing workspace paths, missing editable paths, or missing agent metadata are treated conservatively and remain queued.
- Current concurrency boundary: the project can safely run one MiMo task and one Reasonix TUI task in parallel only when their workspace/editable-path metadata is known and non-overlapping. It does not currently run two MiMo tasks in parallel, and it does not run two Reasonix tasks in parallel. This is intentional safety behavior, not a missing UI toggle.
- Low-cost task routing/model profiles are implemented in P6.25. Further refinements can add a Codex-owned execution target or deeper review strictness, but the first usable Auto/Manual model routing path is complete.
- 2026-06-25 live parallel smoke: Codex started one MiMo task (`task_94caeb3aacb4`) and one Reasonix TUI task (`task_09f498208a7b`) at nearly the same time through safe-client JSON requests. Both returned `running`, proving the queue allows one MiMo plus one Reasonix task concurrently when editable paths do not overlap. Both reached `review`; MiMo wrote `MiMo parallel smoke ok` and Reasonix wrote `Reasonix parallel smoke ok` inside their task Worktrees. The smoke used `runtime/parallel-smoke/...`, which is ignored by Git, so Review Packages correctly showed `changed_files: []` and `NO_CHANGES_AND_NO_TESTS`; future smoke tasks should use tracked test fixture paths, not `runtime/`, when the goal is to validate diff/review-package changed files.
- 2026-06-25 local config issue found during smoke: `%LOCALAPPDATA%\MiMoBridge\config.json` had mojibake Chinese paths in `allowedRoots` and `runtimeDir`, so the first start attempts failed with `路径不在允许的根目录范围内`. Codex backed up the file, added the real repo path, fixed runtimeDir, and restarted the daemon. This is a local machine config issue, not a Git-tracked code change, but it is important for migration/installer validation.
- P6.5 Reasonix reply/continue is implemented locally: `agent_reply_task` is registered in STDIO MCP and HTTP MCP, REST exposes `POST /api/agent-tasks/:id/replies`, and admin UI replies route to the selected task's agent. Reasonix TUI resumes with `reasonix run --resume <agent_session_path>` and validates that the session file is inside configured `REASONIX_HOME`.
- P6.6 Reasonix live/session parser is implemented locally: live view now reads bounded tails from `agent_session_path` for `reasonix-tui` tasks, shows visible assistant replies as primary messages, folds tool calls/results, redacts sensitive path/session/token/password/API-key patterns, skips user/system records, and does not expose `reasoning_content`.
- P6.7/P6.17 safe local-open slices are implemented locally: Admin UI task detail can ask the localhost daemon to open a task folder, Reasonix session folder, or Reasonix GUI companion. The browser sends only fixed actions, not paths or commands; the daemon resolves and validates stored task/config paths before opening, and does not return raw local paths.
- P6.8 Reasonix token/cost extraction is implemented locally: Reasonix session parser extracts explicit `tokens`/`usage`/`token_usage` fields and `ReasonixTuiRunner` records usage into TokenBudget only when real `total_tokens` exists.
- P6.9 generic agent lifecycle tools are implemented locally: `agent_cancel_task`, `agent_finish_task`, `agent_merge_task`, `agent_delete_task`, and `agent_queue_status` are registered in STDIO MCP and HTTP MCP. REST also supports `/api/agent-tasks/:id/cancel`, `/finish`, `/worktree`, `DELETE /api/agent-tasks/:id`, and `GET /api/agent-queue`. These wrap the existing lifecycle handlers, add optional `agent_id` mismatch protection, and let Codex manage Reasonix TUI tasks without using `mimo_*` lifecycle tool names.
- P6.18 Admin UI Agent lifecycle parity is implemented locally: management buttons for non-MiMo tasks now use the generic lifecycle REST routes and include `agent_id`, matching safe-client behavior.
- P6.10/P6.15 safe client Agent commands are implemented locally: `scripts/mimo-bridge-client.mjs` supports `agent-list`, `agent-start`, `agent-wait`, `agent-start-and-wait`, `agent-review`, `agent-cancel`, `agent-finish`, `agent-merge`, `agent-discard`, `agent-delete`, `agent-queue`, and `agent-token-status`. These preserve UTF-8 JSON file/stdin handling and avoid fragile shell JSON for Reasonix tasks. `agent-token-status` is read-only and uses `/api/token-budget`.
- P6.11 safe client reply commands are implemented locally: `reply` and `agent-reply` safely send follow-up messages through fixed REST routes. Short messages can use `--message`; long Chinese or multiline replies should use UTF-8 JSON file/stdin.
- Cross-project MiMo collaboration test on 2026-06-23 fixed the external `Mimo Code 会话管理` tool. Bridge task `task_0a88377ff37d` was delegated to MiMo, waited with `mimo_wait_task`, reviewed with Review Package first, then Codex escalated to focused diff in the target repo and accepted the task.
- External Session Manager committed baseline before this handoff-doc refresh: `master...origin/master [ahead 2]`, latest docs commit `f3fc5efd docs: add session manager handover`, previous code commit `09f70d03 fix: fallback for cleaned Bridge worktree sessions`.

## Completed

- Live viewer enhancement is complete and locally verified: the read-only viewer now surfaces more MiMo-visible text and sanitized tool output summaries from runtime JSONL logs, with wider UI and multiline wrapping. It does not and cannot display hidden model chain-of-thought that MiMo CLI does not emit.

- One localhost daemon serves MCP, REST API, and the React admin UI.
- Codex and the browser share one task queue and runtime state.
- Review defaults to bounded Review Packages; focused diff/file/log reads are explicit escalation.
- Waiting uses `mimo_wait_task` instead of repeated Codex polling.
- Portable package command: `npm.cmd run package:portable`.
- Installer package command: `npm.cmd run package:installer`.
- Release validation command: `npm.cmd run validate:release`.
- Root README and third-party handoff were consolidated into current, short entry points.
- Obsolete historical snapshot docs were removed from the active documentation set.
- Installed launcher `.cmd` generation now writes `MIMO_BRIDGE_DATA_DIR` and `MIMO_BRIDGE_CONFIG` as single-line environment variables.
- Plain double-click installer launch now defaults to quiet install; autostart remains opt-in/off by default.
- Upgrade/reinstall safety fix: `scripts/installer/install.ps1` no longer deletes `app`, `node`, or launcher files until the installed daemon is confirmed stopped. If the daemon or `node-pty` native module is still locked, setup stops with a plain recovery message and preserves the old installation.
- Safe-delete visibility now surfaces backend-derived `can_delete`, `delete_blockers`, and `delete_label` in `/api/tasks` and `/api/tasks/:id`. The admin UI has a `可安全删除` filter and shows delete only when `can_delete` is true.
- Default Chinese display chain: ReviewPackage has optional `objective_zh` / `agent_summary_zh` plus legacy `mimo_summary_zh` fields; admin UI prefers zh for title/objective/summary with English fallback; future task briefs request Chinese summaries. No external translation API.
- Real full-flow retest completed: Codex delegated the safe-delete visibility slice through MCP to MiMo, waited with `mimo_wait_task`, reviewed the bounded Review Package first, escalated only to focused diff for changed files, requested one small MiMo fix, merged the Worktree, and marked the task accepted.
- External Session Manager fallback is complete: `Mimo Code 会话管理` code commit `09f70d03 fix: fallback for cleaned Bridge worktree sessions` lets cleaned Bridge Worktree sessions open by falling back from stale `runtime/worktrees/.../task_xxx` directories to the original `config.workspace_path` stored in `runtime/tasks/task_xxx.json`. Docs commit `f3fc5efd docs: add session manager handover` records the handoff. The EXE at `release/MiMo-Code-Session-Manager.exe` was rebuilt and `python -m unittest -v` passed 14/14.
- P5.4 Safe Agent Invocation is complete: MiMo produced the first cut through task `task_2de8918c60dd`; Codex fixed stale MCP client exit handling, added stronger tests, merged the Worktree, and accepted the task. Verified with `node --test tests/mimo-bridge-client.test.mjs`, `npm.cmd run build`, and real daemon `health/review/wait` smoke.
- TokenBudgetManager real-event hookup is complete: `event-parser` extracts token usage from MiMo `step_finish` events, `mimo-runner` records usage at task completion, and the admin UI text no longer says token events are unconnected. Verified with `node --test tests/event-parser.test.mjs tests/token-budget.test.mjs`, root/local-daemon/admin-ui builds, and a fake-MiMo runner smoke.
- P5.5 Dynamic Task Scope is complete: MiMo implemented `TaskScopePolicy`, per-task scope snapshots, admin UI scope fields, prompt/review-package scope reporting, and out-of-scope rejection. Codex reviewed via bounded Review Package, fixed the legacy empty-scope compatibility case, merged the Worktree, and accepted task `task_d4542e1a2aff`. Verified with `apps/local-daemon` build, root build, and `node --test tests/task-scope.test.mjs tests/review-package.test.mjs tests/prompt-builder.test.mjs tests/admin-api.test.mjs tests/mimo-bridge-client.test.mjs` passing 70/70.
- Origin-thread review handoff is complete in code: MiMo completed task `task_9dd5e0aefba0`; Codex reviewed the bounded Review Package, focused diff, `npm.cmd run build`, `apps/local-daemon` build, and `node --test tests/codex-handoff.test.mjs tests/admin-ui-api.test.mjs tests/admin-api.test.mjs`. Because the main repo had unrelated dirty files, Codex applied the task patch to the main worktree instead of using automatic Worktree merge.
- Pending review recovery inbox is complete in code and tests: `mimo_pending_reviews`/`recover` report only small review summaries and recommended review commands. Verified with `npm.cmd run build`, `apps/local-daemon` build, `node --test tests/mimo-bridge-client.test.mjs tests/admin-api.test.mjs tests/codex-handoff.test.mjs`, and `node --test tests/stdio-protocol.test.mjs`.
- P6.0/P6.1 Agent discovery is complete in code and tests: `agent_list` and `/api/agents` return MiMo plus configured Reasonix TUI probe status. Local real probe against `D:\DeepSeek-Reasonix\bin\reasonix.exe` with `REASONIX_HOME=D:\DeepSeek-Reasonix\ReasonixData` returned `ready`, version `dev`, default model `deepseek`, and redacted provider/session data. Verified with root build, local-daemon build, and `node --test tests/agent-registry.test.mjs tests/admin-api.test.mjs tests/stdio-protocol.test.mjs tests/daemon-config.test.mjs` passing 51/51.
- P6.2 one-shot runner, generic low-token task control, and Reasonix session mapping are complete in code and tests: `agent_start_task(agent_id="reasonix-tui")` creates a Worktree, runs a Reasonix-compatible command, writes bounded visible output to Bridge JSONL logs, updates status to `review`, generates a Review Package with changed files, and persists `agent_session_path` when a Reasonix `.jsonl` session is found under configured `REASONIX_HOME\projects`. `agent_get_task` and `agent_wait_task` expose the same bounded review/wait behavior for any agent with optional `agent_id` mismatch protection. Controlled real smoke task `task_f8b579217015` succeeded with `max_steps=20`; `max_steps=5` was too low and caused a false failure. The smoke task's temporary Worktree was discarded and then marked accepted, so it no longer appears as pending review. Verified with root/local-daemon builds and `node --test tests/reasonix-session-store.test.mjs tests/agent-get-wait-task.test.mjs tests/agent-start-task.test.mjs tests/admin-api.test.mjs tests/stdio-protocol.test.mjs`.
- P6.3 Admin UI first slice is complete: `fetchAgents()` reads `/api/agents`; Create Task has an Agent selector; MiMo still uses `/api/tasks`; Reasonix TUI uses `/api/agent-tasks`; task list/detail show agent badges; System page lists configured agents. Verified with `npm.cmd --prefix apps/admin-ui run build`, root build, local-daemon build, and focused agent/admin tests. Note: tests importing `dist` should not run in parallel with `npm.cmd run build`, or they may read stale compiled output.
- P6.4 Agent-aware queue/path-conflict scheduling is complete in code: `TaskQueue` records `agentId`, `workspacePath`, and `editablePaths`; global concurrency is now 2; different agents can run in parallel only when editable paths are non-overlapping; same-agent tasks and unknown-path/unknown-agent tasks remain queued. Verification should include root/local-daemon/admin-ui builds plus `node --test tests\task-queue.test.mjs tests\concurrent-reject.test.mjs tests\agent-start-task.test.mjs tests\admin-api.test.mjs`.
- P6.5 Reasonix reply/continue is complete in code and focused tests: `agent_reply_task` resumes Reasonix TUI tasks from stored `agent_session_path`, stores the new session path and advances the round counter, refreshes Review Package after the follow-up diff, and keeps browser responses sanitized. Verified with root/local-daemon/admin-ui builds and `node --test tests\agent-reply-task.test.mjs tests\agent-start-task.test.mjs tests\agent-get-wait-task.test.mjs tests\agent-registry.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs tests\task-queue.test.mjs` passing 58/58.
- P6.6 Reasonix live/session parser is complete in code and focused tests: `src/services/reasonix-event-parser.ts` parses bounded Reasonix session JSONL tails; `/api/tasks/:id/live` merges Bridge runtime events and Reasonix session events; Reasonix assistant content is shown, tool events are folded, and hidden reasoning/user prompts/session paths are not exposed. Verified with root build, local-daemon build, admin-ui build, `node --test tests\reasonix-event-parser.test.mjs tests\live-task-view.test.mjs tests\admin-api.test.mjs` passing 61/61, and P6 regression `node --test tests\agent-reply-task.test.mjs tests\agent-start-task.test.mjs tests\agent-get-wait-task.test.mjs tests\agent-registry.test.mjs tests\task-queue.test.mjs tests\stdio-protocol.test.mjs` passing 33/33.
- P6.7/P6.17 safe local-open is complete in code and focused tests: `apps/local-daemon/src/task-open-actions.ts` resolves task folders from active Worktree or original allowed workspace, resolves Reasonix session folders under configured `REASONIX_HOME`, and can open Reasonix GUI through a configured/inferred GUI executable with the shared `REASONIX_HOME`. Admin UI calls `POST /api/tasks/:id/open` through fixed actions. Verified with root build, local-daemon build, admin-ui build, and `node --test tests\task-open-actions.test.mjs tests\admin-api.test.mjs tests\agent-registry.test.mjs` passing 43/43.
- P6.8 Reasonix token/cost extraction is complete in code and focused tests: `extractReasonixTokenUsageFromFile()` reads explicit usage fields from bounded Reasonix session JSONL, `ReasonixTuiRunner` records them to `globalTokenBudget`, and missing fields produce no fake estimates. Verified with root/local-daemon/admin-ui builds and `node --test tests\reasonix-event-parser.test.mjs tests\agent-start-task.test.mjs tests\token-budget.test.mjs` passing 28/28.
- P6.9 generic agent lifecycle is complete in code and focused tests: `agent_cancel_task`, `agent_finish_task`, `agent_merge_task`, `agent_delete_task`, and `agent_queue_status` are available over STDIO/HTTP MCP; `/api/agent-tasks/:id` supports cancel/finish/worktree/delete lifecycle actions; `/api/agent-queue` supports optional agent filtering. Verified with root/local-daemon/admin-ui builds, `node --test tests\agent-lifecycle-task.test.mjs tests\agent-get-wait-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passing 43/43, and P6 regression `node --test tests\agent-start-task.test.mjs tests\agent-reply-task.test.mjs tests\task-queue.test.mjs tests\reasonix-event-parser.test.mjs tests\live-task-view.test.mjs tests\token-budget.test.mjs` passing 75/75.
- P6.18 Admin UI lifecycle parity is complete in code and focused tests: `cancelTask`, `finishTask`, `worktreeTask`, and `deleteTask` in `apps/admin-ui/src/api.ts` choose `/api/agent-tasks/:id/...` for non-MiMo tasks and `/api/tasks/:id/...` for MiMo tasks. `TaskDetailPage` passes the selected task's agent into lifecycle handlers. Verified with root/local-daemon/admin-ui builds and `node --test tests\admin-ui-lifecycle-api.test.mjs tests\agent-lifecycle-task.test.mjs tests\admin-api.test.mjs` passing 37/37.
- P6.9 is deployed to the local daemon: `launcher.ps1 restart` timed out in the shell, but `launcher.ps1 status -Json` showed a new PID 25948 and healthy MCP. `recover` returned no pending reviews, and `GET /api/agent-queue?agent_id=reasonix-tui` returned ok with an empty queue.
- P6.10 safe client Agent commands are complete in code and focused tests: `node --test tests\mimo-bridge-client.test.mjs` passed 24/24, and combined focused regression `node --test tests\mimo-bridge-client.test.mjs tests\agent-lifecycle-task.test.mjs tests\agent-get-wait-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 67/67 after root/local-daemon/admin-ui builds.
- P6.10 live daemon smoke passed: `agent-list` returned ready MiMo and ready Reasonix TUI, and `agent-queue --agent-id reasonix-tui` returned an empty queue.
- P6.11 safe client reply commands are complete in code and focused tests: `node --test tests\mimo-bridge-client.test.mjs` passed 26/26, and combined focused regression `node --test tests\mimo-bridge-client.test.mjs tests\agent-reply-task.test.mjs tests\agent-lifecycle-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 67/67 after root/local-daemon/admin-ui builds.
- Latest package build after P6.17 is complete: `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,224 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,531 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer self-test exit was 0 and release validation wrote ignored local `artifacts\release-validation.json`.

## Collaboration Needed

- Codex: keep planning/reviewing and use low-token wait/review rules.
- MiMo: execute bounded coding tasks inside allowed paths and task Worktrees.
- Third-party agents: use `PROJECT_MEMORY.md`, `AGENTS.md`, and this handover before touching code.
- External Session Manager docs: if continuing the local session manager tool, read `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理\docs\HANDOVER_STATUS.md` and `docs\modules\bridge-session-fallback.md`.

## Remaining Work

1. Run and record `docs/RELEASE_VALIDATION.md` on clean Windows 10/11 x64 machines. The user has tried the installer on a company computer, but the formal checklist still needs written evidence.
2. Validate double-click installer, upgrade over a running old daemon, portable ZIP, reboot/logon, no system Node, port conflict, first-run config, Codex MCP connection, real MiMo task, autostart opt-in, and uninstall behavior.
3. Audit active Worktree cancellation cleanup.
4. Continue P6 multi-agent dispatch for MiMo plus Reasonix. P6 target is Reasonix reaching MiMo-level Bridge capability, with Reasonix TUI as the first execution path and Reasonix GUI limited to shared-session/manual companion viewing unless a stable deep link appears. P6 must reuse P5.5 `TaskScopePolicy`. Next engineering slices are deeper MiMo adapter migration behind the generic `agent_*` layer and final Reasonix parity validation.
5. Rebuild installer/portable artifacts after the next runtime slice, then run release validation.
6. Continue safe session opening only if Reasonix exposes a stable command/deep link for a specific session. Current support opens task folders, Reasonix session folders, and Reasonix GUI with the shared `REASONIX_HOME`; it does not promise session-specific GUI focus.
7. Later, refine low-cost routing if needed: add a Codex-owned execution target, richer complexity rules, or stricter review behavior based on scenario/risk. The first Auto/Manual model routing implementation is complete in P6.25.

## Risks / Blockers

- Live viewer boundary: it can display visible MiMo text and sanitized tool output that exists in JSONL logs; it cannot display private/hidden model reasoning that the CLI does not emit.

- `tests/runner-integration.test.mjs` hangs on Windows and is excluded from normal regression.
- Windows PTY tests can print `AttachConsole failed` and `TimeoutNaNWarning`; judge by exit code and regression result.
- Codex shell capture can lose direct stdout when launcher commands spawn the daemon; verify daemon health with `launcher.ps1 status -Json`.
- Launcher restart can refuse to stop an unmanaged/ambiguous daemon if Windows does not expose the process command line. That is intentional safety behavior. In that case, verify the PID/owner before stopping, or restart from the installed launcher/after reboot.
- During the 2026-06-22 full-flow retest, the first `mimo_wait_task(timeout_seconds=600)` call used the MCP SDK default 60s request timeout and failed client-side. P5.4's safe client now sets the request timeout longer than `timeout_seconds`; any custom SDK caller outside the safe client must still do the same.
- During the 2026-06-23 delegation test, several startup attempts failed before MiMo received a task because PowerShell/inline Node command strings mishandled Chinese paths or special characters. P5.4 mitigates this with a UTF-8 JSON file/stdin client wrapper; do not regress to inline JSON shell commands.
- When delegating to projects outside the Bridge repo, the folder must be present in Bridge `allowedRoots`. This machine now has `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理` in `%LOCALAPPDATA%\MiMoBridge\config.json`; reproduce that setting on migrated machines before asking MiMo to modify the session manager.
- Review Package can show `changed_files: []` for `use_worktree=false` cross-project tasks even when the target repo changed. In that case, escalate to focused `git diff` inside the target repo instead of assuming no changes.
- Clean Windows validation is still the main external blocker before calling the package ready for general use.
- The local machine install path and automated installer self-test pass, but a separate clean Windows 10/11 validation pass is still required. Upgrade-over-running-daemon should be manually checked there too.

## Recommended Next Action

After context compression, read `PROJECT_MEMORY.md` first, then this file, then `docs/OPEN_TASKS.md`. If the next task concerns the local Session Manager, also read `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理\docs\HANDOVER_STATUS.md` and `docs\modules\bridge-session-fallback.md`.

Next engineering action: continue with deeper MiMo adapter migration behind generic tools, then run final Reasonix parity/release validation. Only revisit direct Reasonix GUI session focus if a stable command/deep link is discovered. For further scripted MiMo/Reasonix collaboration, use ``scripts/mimo-bridge-client.mjs``/``.ps1`` where available, run ``recover`` after any interrupted wait, keep using Review Package first, and escalate to focused diff only when risk flags, ``use_worktree=false``, or review notes require it.

