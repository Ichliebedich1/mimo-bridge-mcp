# MiMo Bridge MCP Long-Term Memory

## Active Checkpoint 2026-06-26

- P6.26 goal completed: real MiMo + Reasonix collaboration flow finished the five requested items and produced the final EXE. Completed items: stronger task-complete wake/recovery; Admin UI pasted image and file attachment support; MiMo safe CMD session open action; Reasonix TUI GUI/session fallback open action; commit after each completed slice; final installer EXE build.
- Current live checks after final packaging: daemon health ok on 127.0.0.1:3210, MiMo and Reasonix configured, queue empty, `node scripts\mimo-review-wakeup.mjs --limit 5 --max-chars 8000` returned `pending_count: 0`, git worktree clean before this doc refresh, branch `master` is ahead of origin by 6.
- Final P6.26 artifacts: `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` is 55,631,872 bytes and `artifacts\MiMoBridge-portable-win10-win11-x64.zip` is 55,665,889 bytes. `npm.cmd run validate:release -- -SkipPackageBuild` passed during final packaging.

This file is the project-local long-term memory. Update it after each meaningful phase so another agent can continue without relying on chat context.

- 2026-06-26 current operating rule from user: stop using Reasonix for implementation for now; review MiMo code and let MiMo take over Reasonix's unfinished work only after the current worktree is stable. Preserve three reliability notes: safe-client JSON BOM failures must stay regression-tested, completion/failure wakeup must be improved so Codex does not dead-wait after Agent terminal states, and Reasonix TUI is currently unreliable enough to pause until a dedicated reliability pass.

## Latest Status

- 2026-06-26 current goal slice 1 completed: safe-client JSON input now strips a leading UTF-8 BOM for both `--json` files and stdin before `JSON.parse`. This fixes the repeated `Invalid JSON input: Unexpected token '﻿'` failure where Codex-created request files failed before reaching MiMo/Reasonix. Verification passed: `node --test tests\mimo-bridge-client.test.mjs` with 31/31 tests, including new BOM file/stdin regressions. Remaining current-goal items: stronger completion/failure wakeup, Admin UI task-detail side task list, collapsible task-list details, batch delete, Reasonix reliability, reply component placement with upload support, and Ultra Speed toggle persistence.
- 2026-06-26 Ultra Speed collaboration incident and recovery: while delegating MiMo Ultra Speed support, Codex again generated safe-client JSON request files with a UTF-8 BOM. `scripts/mimo-bridge-client.mjs start --json ...` rejected the file before MiMo received the task. This is an invocation-layer failure, not an Agent failure. It must be fixed in the Safe Agent Invocation path by either accepting/stripping BOM in the safe client or providing a project script/helper that always writes UTF-8 no-BOM request files, with regression tests. Same run exposed that the current wait/wakeup story is still not good enough: one Agent task completed and one failed, but Codex stayed blocked in a long wait instead of being promptly reactivated for review/intervention. Treat "task completed/failed wakes Codex or returns to recover path" as a must-fix reliability item, not polish. Reasonix TUI also failed again on delegated UI task `task_ce0745c6b01e`; its Worktree was discarded and the task was marked abandoned. Pause Reasonix for implementation work until there is a dedicated reliability pass. MiMo backend task `task_104155a74501` was reviewed, manually absorbed, focused-tested, its Worktree discarded, and the task marked accepted. Backend support now exists for `mimo-v2.5-pro-ultra-speed`, default-off `enable_mimo_pro_ultra_speed`, pricing metadata, CLI model mapping, multimodal rejection, and stale-Ultra cleanup when the switch is explicitly turned off. Verification: `npm.cmd run build` and `node --test tests\model-routing.test.mjs tests\mimo-runner.test.mjs tests\admin-api.test.mjs` passed 57/57. Next step: use MiMo only to implement the Admin UI switch/save/dropdown support.
- 2026-06-26 Ultra Speed UI completed: MiMo task `task_08d79ed33e76` implemented the Admin UI switch/save/dropdown support, Codex reviewed it from the bounded Review Package and focused diff, added a small old-daemon price fallback, manually absorbed it, discarded the task Worktree, and marked the task accepted. UI now has a default-off Chinese switch "启用 MiMo V2.5 Pro Ultra Speed（内测，高速高价）", saves `enable_mimo_pro_ultra_speed`, shows Ultra Speed only when enabled, removes stale Ultra selections when disabled, and preserves multimodal flash-only behavior. Verification: `npm.cmd --prefix apps\admin-ui run build` passed and `node --test tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs tests\model-routing.test.mjs tests\mimo-runner.test.mjs` passed 63/63. `/api/pending-reviews` returned `pending_count: 0`.
- 2026-06-26 Ultra Speed final packaging: `npm.cmd run package:installer` refreshed the Windows installer and portable ZIP; `npm.cmd run validate:release -- -SkipPackageBuild` passed. Current artifacts: `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` 55,632,896 bytes and ignored `artifacts\MiMoBridge-portable-win10-win11-x64.zip` 55,667,036 bytes. Installer manifest source commit is `da09549`.
- 2026-06-26 open-source positioning update: the public project/product name is now **AgentBridge Local**. Former name `MiMo Bridge MCP` is kept only as historical context and for compatibility with existing package/artifact names. README, Chinese README, GitHub publication checklist, release notes, demo launch copy, good-first-issues, contributing guide, changelog, and package description were rewritten toward a GitHub landing-page style: Codex plans/reviews; MiMo/Reasonix execute bounded tasks in isolated Git Worktrees; Review Package is the low-token review primitive; Windows 10/11 x64 remains the first target. Artifact names such as `MiMoBridgeSetup-win10-win11-x64.exe` are intentionally unchanged for now.
- P6.26 slice 3 completed on 2026-06-26: task-completion wake/recovery is now formalized as "bounded recovery inbox + Codex App heartbeat" rather than a daemon-native hard push into a stopped Codex turn. Added `scripts/mimo-review-wakeup.mjs`, which runs MiMo `recover` and Reasonix `agent-recover` once, deduplicates tasks, and prints only task IDs, agent/status, risk flags, recommendation, and next review command. The current Codex heartbeat automation `mimo-bridge-review-wakeup` was updated to call this script every 5 minutes with readable Chinese instructions. This keeps Codex from repeatedly polling task state while still surfacing completed/failed tasks after interruptions.
- P6.26 MiMo model compatibility fix: MiMo CLI requires `--model provider/model`. Bridge still stores and displays user-facing models `mimo-v2.5-flash` / `mimo-v2.5-pro`, but `mimo-runner` now maps them to CLI-safe names before execution: `mimo-v2.5-flash -> xiaomi/mimo-v2.5`, `mimo-v2.5-pro -> xiaomi/mimo-v2.5-pro`. Reasoning effort also maps to MiMo `--variant`: low=minimal, medium=high, high=max. This fixes the historical `ProviderModelNotFoundError` seen in task `task_8ee96c2c4189`, where the CLI interpreted `mimo-v2.5-flash` as a provider ID.
- P6.26 slice 3 verification so far: `npm.cmd run build`, `node --test tests\mimo-runner.test.mjs tests\model-routing.test.mjs tests\agent-start-task.test.mjs`, and `node scripts\mimo-review-wakeup.mjs --limit 5 --max-chars 8000` passed. Pending historical tasks at this point are old collaboration drafts only: MiMo `task_8ee96c2c4189` failed to run because of the model format issue and has no code changes; Reasonix `task_dcd3b5a75a14` failed after partial UI edits that were later manually reimplemented and committed.
- Historical pending task cleanup completed on 2026-06-26: after Review Package inspection, Codex safely discarded Worktrees, marked abandoned, and deleted old task records for MiMo `task_8ee96c2c4189` and Reasonix `task_dcd3b5a75a14`. `node scripts\mimo-review-wakeup.mjs --limit 5 --max-chars 8000` and `health.pending_reviews.count` now report 0.
- P6.26 final packaging completed on 2026-06-26: `npm.cmd run package:installer` regenerated `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,631,872 bytes) and ignored portable ZIP `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,665,889 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed, including installer self-test. Installer manifest source commit is `1fdeb68`.
- P6.26 slice 2 completed on 2026-06-26: Create Task now supports pasted screenshots and selected file attachments. The Admin UI reads clipboard/files in the browser, stores only attachment metadata plus base64 in the create request, shows an attachment list with type/size/remove controls, and auto-switches image attachments to multimodal Auto routing. The daemon accepts bounded attachment payloads, persists them under `runtime/attachments/<task_id>/`, sanitizes filenames, validates base64/size, stores safe attachment metadata in task config, and task briefs list the saved attachment paths for MiMo/Reasonix. Raw browser local paths are never sent. Verification passed: root build, local-daemon build, admin-ui build, `node --test tests\task-attachments.test.mjs tests\prompt-builder.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs`.
- P6.26 slice 1 completed on 2026-06-26: Admin UI task detail now has fixed safe terminal actions for sessions. MiMo tasks can show "在 CMD 打开 MiMo 会话"; Reasonix TUI tasks can show "在 CMD 打开 Reasonix 会话" alongside session-folder and GUI buttons. Backend action names are fixed (`mimo_session_terminal`, `reasonix_session_terminal`); the browser still cannot pass arbitrary local paths or arbitrary commands. The daemon validates MiMo `session_id`, configured MiMo node/entry paths, Reasonix `agent_session_path` under configured `REASONIX_HOME`, and allowed workspace/worktree fallback before launching `cmd.exe /k`. Verification passed: root build, local-daemon build, admin-ui build, `node --test tests\task-open-actions.test.mjs`, `node --test tests\admin-api.test.mjs`, and `node --test tests\admin-ui-lifecycle-api.test.mjs`.
- P6.25 low-cost model routing/profile configuration is implemented locally. The Bridge now has shared routing profiles for `multimodal`, `simple`, `normal`, `complex`, and `high_risk`; Admin UI has a "模型路由" settings page; Create Task supports scenario, Auto/Manual routing, image/multimodal hint, Agent/model/reasoning-effort controls, and preview. Routing config is persisted through `/api/routing-profiles`, written into task config, task briefs, and Review Package, and passed to runners where supported: MiMo receives `--model`, Reasonix receives `--model` and maps `low/medium/high` to `--max-steps` 10/20/40. Model facts are enforced: MiMo allows `mimo-v2.5-flash` / `mimo-v2.5-pro`; Reasonix allows `deepseek-v4-flash` / `deepseek-v4-pro`; multimodal is forced to MiMo `mimo-v2.5-flash`. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\model-routing.test.mjs tests\agent-start-task.test.mjs tests\admin-api.test.mjs tests\review-package.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-ui-task-actions.test.mjs` passed 74/74.
- P6.25 collaboration cleanup note: the Reasonix UI routing task `task_c27e97cfd1d6` and MiMo backend routing task `task_158531fc17eb` were useful but were not merged directly. Reasonix's UI draft used an older scenario taxonomy, and the MiMo backend task was interrupted before a session id was recorded. Codex manually absorbed the useful backend/UI ideas into the main worktree, then discarded both old Worktrees and marked both tasks abandoned. `recover` / `agent-recover --agent-id reasonix-tui` returned no pending tasks afterward. Do not start duplicate routing tasks from those IDs.
- P6.24 failed-task reply visibility and missed-intervention UX is implemented locally. The immediate Reasonix TUI failure was not an out-of-scope code change: `task_c27e97cfd1d6` hit `agent.max_steps` / exit code 1 after partial Admin UI edits, kept a valid `agent_session_path`, and is resumable. A second failed MiMo task `task_158531fc17eb` was interrupted before recording a MiMo session ID, so it cannot be replied to and must be reviewed/discarded manually. The bridge now computes explicit reply capability per task (`can_reply`, `reply_blockers`, `reply_label`) in the Admin API list/detail responses. Admin UI no longer guesses only from status: it shows whether a failed task can continue, why a task cannot be replied to, and a top-bar/overview "待介入" count from `health.pending_reviews.count`. The old "建议拒绝或要求 MiMo 继续修复" copy was generalized to "执行 Agent". Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\task-reply-capability.test.mjs tests\admin-api.test.mjs tests\pending-reviews.test.mjs tests\agent-reply-task.test.mjs tests\admin-ui-task-actions.test.mjs` passed 43/43.
- Operational lesson: Codex did not "see" this failure immediately because daemon completion/failure does not push an active wake event into the current Codex turn. Existing recovery/heartbeat makes it discoverable, but true instant recovery still needs a stronger app-side wakeup/notification mechanism. Until then, always run `recover` / `agent-recover` after interruptions and watch the Admin UI `待介入` count.
- P6.23 failed-task recovery/continue is implemented locally. Root cause from the Reasonix UI-routing task: `reasonix run` stopped with `paused after 20 tool-call rounds (agent.max_steps)`, leaving partial Worktree edits and status `failed`; the previous recovery inbox only returned `review` tasks, so Codex did not get pulled back to intervene. The recovery inbox now includes `failed` tasks that have errors/issues/risk flags/Worktrees and terminal cancelled/abandoned tasks with residual Worktrees. Failed tasks can now be replied to, and the Admin UI reply box is enabled for `failed` status so Reasonix can continue from its recorded session. Verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\pending-reviews.test.mjs tests\agent-reply-task.test.mjs tests\admin-ui-task-actions.test.mjs tests\mimo-bridge-client.test.mjs tests\admin-api.test.mjs` passed 68/68. Local daemon was restarted; `health` is ready and live `agent-recover --agent-id reasonix-tui` now returns failed task `task_c27e97cfd1d6` for Codex intervention.
- Caution from the same run: do not restart the local daemon while MiMo/Reasonix tasks are running unless necessary. Restarting the daemon interrupted MiMo backend-routing task `task_158531fc17eb`, which now appears as a failed task with a populated Worktree and should be reviewed/replied/discarded through the low-context recovery path.

- P6.22 terminal Worktree cleanup is implemented and deployed locally. Failed, cancelled, and abandoned tasks that still have a Worktree can now use the Admin UI "丢弃 Worktree 并放弃" action; the backend allows those terminal states to be marked `abandoned` after discard while still rejecting failed-task acceptance. Discard also handles stale records where the Worktree directory was already removed externally: it validates the stored Worktree root/task-id shape, prunes stale Git worktree metadata, deletes the task branch best-effort, and clears the task's Worktree record. Admin UI action buttons are grouped into "审查结论", "清理 Worktree", and "队列与删除". Verification passed: root build, local-daemon build, admin-ui build, `node --test tests\finish-task.test.mjs tests\merge-task.test.mjs tests\admin-ui-task-actions.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs` passed 42/42, and the local daemon was restarted healthy on port 3210. Note: active/running Worktree cancellation cleanup remains a separate audit item; this fix covers terminal failed/cancelled/abandoned cleanup.
- 2026-06-25 checkpoint before multi-agent full-flow smoke: user wants configurable default routing policies in the MiMo admin UI for scenarios such as multimodal/image, simple text/docs/UI, normal code, complex runtime/Git/installer/security-boundary work, and high-risk tasks. The UI should allow manual Agent/model/reasoning-effort selection and an Auto mode backed by Codex/Bridge rules. Model facts supplied by user: MiMo supports `mimo-v2.5-flash` and `mimo-v2.5-pro`; only MiMo flash supports multimodal. Reasonix supports `deepseek-v4-flash` and `deepseek-v4-pro`; neither supports multimodal. Flash pricing is input ¥1/output ¥3/cache-hit ¥0.02 per 1M, pro pricing is input ¥3/output ¥6/cache-hit ¥0.025 per 1M. MiMo CLI help confirms `run --model` and `--variant`; Reasonix `run --help` confirms `--model` and `--max-steps`, but no dedicated reasoning-effort parameter, so first implementation should map Reasonix effort through max_steps/metadata rather than `--show-thinking`.
- 2026-06-25 full-flow smoke target: verify one MiMo task and one Reasonix TUI task can run concurrently when they use non-overlapping editable paths. Current daemon health is ready, queue is empty, `recover` and `agent-recover --agent-id reasonix-tui` both returned no pending reviews, and `agent-list` reported both MiMo and Reasonix ready. Use safe-client JSON files, one-shot long waits, Review Package first, and focused escalation only if risk flags appear.
- Installer upgrade compatibility fix is implemented and packaged for the company-PC issue where the EXE stayed at 0 percent while text flashed. The installer can now stop an old daemon without `launcher-state.json` only when the port owner command line proves it is the installed MiMo Bridge daemon under `%LOCALAPPDATA%\MiMoBridgeApp`; otherwise it preserves the old install and tells the user to close MiMo Bridge or reboot.
- Release artifacts were refreshed after installer upgrade compatibility fix. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,608,320 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,642,150 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `f3d61e3`.
- P6.21 Admin UI open actions are implemented locally. `/api/agent-tasks/:id/open` now supports fixed local-open actions for non-MiMo tasks after an `agent_id` guard; the Admin UI routes Reasonix open-task-folder/session-folder/GUI actions through the generic Agent route while MiMo keeps `/api/tasks/:id/open`. Browser clients still send only fixed actions, never arbitrary paths or commands.
- P6.21 verification passed: root build, local-daemon build, admin-ui build, `node --test tests\admin-ui-lifecycle-api.test.mjs`, and `node --test tests\admin-api.test.mjs tests\task-open-actions.test.mjs tests\agent-get-wait-task.test.mjs` passed 43/43.
- P6.20 Admin UI task-detail reads are implemented locally. Detail refresh, focused file read, diff read, log tail, and full debug helpers now route non-MiMo tasks through `/api/agent-tasks/:id?...agent_id=...`, while MiMo tasks keep `/api/tasks/:id?...`. This removes another management-console dependency on MiMo-only read routes for Reasonix task review.
- P6.20 verification passed: admin-ui build, root build, and `node --test tests\admin-ui-task-detail-api.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs tests\agent-get-wait-task.test.mjs` passed 37/37.
- Release artifacts were refreshed after P6.20. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,925 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `692a8d8`.
- P6.19 Admin UI Codex handoff is implemented locally. The "交给 Codex 审查" prompt is now agent-aware: MiMo tasks keep the `mimo_get_task` / safe-client `review` path, while Reasonix TUI tasks tell Codex to use `agent_get_task(agent_id="reasonix-tui", ...)` or `agent-review --agent-id reasonix-tui`. The task detail reply box also displays the actual agent name instead of always saying "回复 MiMo".
- P6.19 verification passed: admin-ui build, root build, `node --test tests\codex-handoff.test.mjs tests\admin-ui-api.test.mjs`, and `node --test tests\codex-handoff.test.mjs tests\admin-ui-api.test.mjs tests\admin-ui-lifecycle-api.test.mjs tests\admin-api.test.mjs` passed 51/51.
- Release artifacts were refreshed after P6.19. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,860 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `d5749d3`.
- P6.18 Admin UI Agent lifecycle parity is implemented locally. The management UI now routes non-MiMo cancel/finish/worktree/delete actions through `/api/agent-tasks/:id/...` with `agent_id`, while MiMo tasks keep the legacy `/api/tasks/:id/...` paths. This aligns UI lifecycle operations with the generic MCP/safe-client lifecycle tools already added in P6.9-P6.10.
- P6.18 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\admin-ui-lifecycle-api.test.mjs tests\agent-lifecycle-task.test.mjs tests\admin-api.test.mjs` passed 37/37.
- Release artifacts were refreshed after P6.18. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,736 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,641 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `78dffac`.
- P6.17 Reasonix GUI companion opening is implemented locally. Reasonix task detail now has a fixed Admin UI action for opening Reasonix GUI through the localhost daemon; the browser sends only `action=reasonix_gui`, never a local path or command. The daemon uses an explicit `reasonix-gui` agent command when configured, otherwise safely infers `...\ReasonixDesktop\reasonix-desktop.exe` from the configured Reasonix TUI `home_dir`/command and launches it with the same `REASONIX_HOME`.
- P6.17 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\task-open-actions.test.mjs tests\admin-api.test.mjs tests\agent-registry.test.mjs` passed 43/43. This does not mean Reasonix can deep-link to a specific session; `reasonix --help` exposes no stable GUI session-open argument, so GUI opening remains shared-home/manual viewing.
- Release artifacts were refreshed after P6.17. `npm.cmd run package:installer` produced `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,224 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,531 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed; installer manifest source commit is `1bafa35`.
- P6.16 generic Review Package summary is implemented locally. Review Packages now add `agent_summary` / `agent_summary_zh` while preserving legacy `mimo_summary` / `mimo_summary_zh` for old clients and UI compatibility. Admin UI prefers the generic summary when available.
- P6.16 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\review-package.test.mjs tests\agent-start-task.test.mjs tests\admin-api.test.mjs` passed 51/51.
- P6.15 generic Agent token status is implemented and deployed locally. New surfaces: MCP `agent_token_status` and safe client `node scripts\mimo-bridge-client.mjs agent-token-status`. Both use the shared TokenBudget data that MiMo and Reasonix already write to; the safe client command is read-only and does not expose reset.
- P6.15 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\mimo-bridge-client.test.mjs tests\stdio-protocol.test.mjs tests\token-budget.test.mjs` passed 52/52. Daemon was restarted; live `health`, `agent-token-status`, and `agent-list` smoke passed.
- P6.12 bounded generic Agent task listing is implemented and deployed locally. New surfaces: MCP `agent_list_tasks`, REST `GET /api/agent-tasks?agent_id=...&limit=...`, and safe client `node scripts\mimo-bridge-client.mjs agent-tasks --agent-id reasonix-tui --limit 5`.
- The default list output is low-context by design: task id, agent, status, objective, sanitized/truncated summary, modified file count, risk flags, review recommendation, timestamps, round, Worktree state, and safe-delete metadata. It does not return full diff, full logs, raw source, raw log paths, or raw local paths.
- P6.12 verification passed: `npm.cmd run build`, `npm.cmd --prefix apps\local-daemon run build`, `npm.cmd --prefix apps\admin-ui run build`, and `node --test tests\agent-list-tasks.test.mjs tests\mimo-bridge-client.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 65/65.
- Local daemon was restarted after P6.12. `agent-list`, `agent-queue --agent-id reasonix-tui`, and `agent-tasks --agent-id reasonix-tui --limit 5` live smoke passed; historical Reasonix summaries were sanitized and truncated.
- P6.13 generic Agent pending-review recovery is implemented and deployed locally. New surfaces: MCP `agent_pending_reviews`, REST `GET /api/agent-pending-reviews?agent_id=...&limit=...&max_chars=...`, and safe client `node scripts\mimo-bridge-client.mjs agent-recover --agent-id reasonix-tui --limit 5 --max-chars 8000`.
- P6.13 gives Reasonix the same interrupted-wait recovery path as MiMo: Codex can ask for completed Reasonix tasks waiting for review without reading full logs, full diff, source, or local paths. Reasonix summaries generate `agent-review --agent-id reasonix-tui ...` commands; MiMo summaries keep the old `review --task-id ...` command.
- P6.13 verification passed: root build, local-daemon build, admin-ui build, and `node --test tests\pending-reviews.test.mjs tests\mimo-bridge-client.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 65/65. Live `agent-recover --agent-id reasonix-tui` returned ok with no pending reviews.
- P6.14 token capability copy fix is implemented locally. Ready Reasonix TUI now reports `capabilities.token_usage=true` because `ReasonixTuiRunner` records explicit session JSONL token/cost fields into the shared TokenBudget. The Admin UI Token page now says MiMo and Reasonix both contribute only real logged token/cost data; it no longer implies token tracking is MiMo-only.
- P6.14 verification passed after sequential build then tests: root build, local-daemon build, admin-ui build, and `node --test tests\agent-registry.test.mjs tests\agent-start-task.test.mjs tests\token-budget.test.mjs` passed 24/24. Do not run dist-importing tests in parallel with `npm.cmd run build`, or they can read stale compiled output.

## Current Release Target

- Target OS: Windows 10/11 x64.
- Distribution style: portable ZIP plus EXE installer.
- Runtime model: one localhost-only Node daemon shared by Codex MCP and the React admin UI.
- Internal MCP endpoint: http://127.0.0.1:3210/mcp.
- Autostart: off by default; only enable when the user explicitly opts in.
- Do not bundle MiMo credentials, MiMo login state, active tasks, runtime logs, or Git Worktrees.
- MiMo Code must be installed and logged in separately on each computer.

## Current Implementation State

- P0-P5.2 are implemented.
- P5.3 portable ZIP exists through npm.cmd run package:portable.
- P5.3 EXE installer is implemented through npm.cmd run package:installer.
- The installer is per-user: default app files go under LOCALAPPDATA\\MiMoBridgeApp; user data stays under LOCALAPPDATA\\MiMoBridge.
- Local package:installer build succeeds and produces the portable ZIP plus EXE installer. The user has also tried installing on a company computer, but the formal clean Windows 10/11 x64 release checklist is not fully recorded yet.
- Installer upgrade hardening is implemented: unknown setup flags are rejected, `-Help` is supported, reinstall verifies the old daemon is stopped before replacing files, locked native/runtime files abort safely, replacement uses staging plus rollback, setup logs go under LOCALAPPDATA\\MiMoBridge\\setup.log, and `-SelfTest` starts a temporary daemon to verify `/api/health` plus Admin UI HTML without installing.
- P5.4/P6.10-P6.15 Safe Agent Invocation is implemented through `scripts/mimo-bridge-client.mjs` and `scripts/mimo-bridge-client.ps1`.
- TokenBudgetManager is connected to real MiMo JSONL `part.tokens` / `part.cost` events and to Reasonix session JSONL token/cost fields when they exist. Reasonix token usage is never estimated from text; if no explicit `usage`/`tokens` fields exist, Bridge records nothing.
- P6.0/P6.1 first slice is implemented locally: Agent Registry, `agent_list`, `/api/agents`, daemon health agent summary, and Reasonix TUI probe support. `reasonix-gui` is recognized as a viewer-only companion when configured; it is not a task execution agent.
- P6.2 runner/control/session slice is implemented locally: `agent_start_task`, `agent_get_task`, `agent_wait_task`, `/api/agent-tasks`, `/api/agent-tasks/:id`, `/api/agent-tasks/:id/wait`, and `ReasonixTuiRunner` can run and review one-shot Reasonix-style tasks through the existing Worktree, queue, scope, low-token wait, Review Package flow, and Reasonix session mapping. Verified with fake Reasonix and a controlled real Reasonix smoke; real smoke needed `max_steps=20` because `max_steps=5` caused a false failure.

## Must-Preserve Architecture

- Keep the existing React admin UI and Node local daemon.
- Do not introduce Electron, Tauri, cloud hosting, LAN exposure, or a second task runtime without a new explicit decision.
- Codex plans/reviews/accepts; MiMo executes bounded coding tasks.
- MiMo must not merge its own Worktree.
- Review uses bounded Review Packages first; only read focused diff/files/logs when risk requires it.
- Waiting uses mimo_wait_task instead of repeated polling.

## Known Technical Debt

- tests/runner-integration.test.mjs hangs on Windows and is excluded from normal regression.
- Windows PTY tests can print AttachConsole failed and TimeoutNaNWarning; treat as noise only when tests exit 0.
- Active Worktree cancellation cleanup needs a focused audit.
- Clean-machine double-click, reboot/logon, no-system-Node, port-conflict, and uninstall validation are pending.
- MCP SDK callers must pass a request timeout longer than `mimo_wait_task.timeout_seconds`; otherwise the client can time out even though daemon-side low-token waiting is working. Documented in `docs/modules/low-token-wait.md` with 1800/3600s examples.
- Ad hoc inline Node/PowerShell agent-to-bridge calls should be replaced by the P5.4 safe client wrapper. Do not regress to command-line JSON construction.

## Next Handoff Checklist

1. Read this file first.
2. Run git status --short --branch.
3. Read docs/HANDOVER_STATUS.md, docs/OPEN_TASKS.md, and docs/modules/windows-launcher-portability.md.
4. If the issue involves the external Session Manager, read:
   - `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理\docs\HANDOVER_STATUS.md`
   - `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理\docs\modules\bridge-session-fallback.md`
5. Use npm.cmd run package:portable for the portable package.
6. Use npm.cmd run package:installer for the EXE installer.
7. Use npm.cmd run validate:release for automated local release validation, or npm.cmd run validate:release -- -SkipPackageBuild after artifacts already exist.
8. Use docs/RELEASE_VALIDATION.md for clean Windows 10/11 x64 manual validation.
9. Run focused tests before broad regression:
   - node --test tests/installer-package.test.mjs
   - node --test tests/launcher-controller.test.mjs
10. Normal regression must continue excluding tests/runner-integration.test.mjs.

## Active Documentation Set

- Primary entry points: README.md, PROJECT_MEMORY.md, AGENTS.md, docs/HANDOVER_STATUS.md, and docs/OPEN_TASKS.md.
- Architecture and module references: docs/ARCHITECTURE.md, docs/MODULE_MAP.md, docs/DECISIONS.md, docs/PROJECT_BRIEF.md, docs/RELEASE_VALIDATION.md, and docs/modules/*.md.
- Third-party takeover entry: docs/THIRD_PARTY_AGENT_HANDOFF.md is now a short index, not a duplicated long-form state dump.
- Removed from active docs during cleanup: root HANDOFF.md, root PROJECT.md, and docs/UI_DEVELOPMENT.md. Do not depend on those paths for current facts.
- Current release artifacts stay ignored under artifacts/. The portable staging directory artifacts/portable/MiMoBridge is kept because validate-release.ps1 -SkipPackageBuild reads it.

## Latest Notes

- Context-compression checkpoint on 2026-06-23: committed baseline before this handoff-doc refresh was Bridge `master...origin/master [ahead 1]` at `01d734f docs: record session manager fallback collaboration`, and external Session Manager `master...origin/master [ahead 2]` at `f3fc5efd docs: add session manager handover` with previous code commit `09f70d03 fix: fallback for cleaned Bridge worktree sessions`. This checkpoint may itself be an uncommitted documentation edit if the user did not ask for a Git commit. Re-run `git status --short --branch` after resuming.
- Cross-project Session Manager repair completed on 2026-06-23. User issue: MiMo sessions created by Codex/MiMo Bridge could appear in `Mimo Code 会话管理` but fail to open after their Bridge Worktree was merged/cleaned. MiMo task `task_0a88377ff37d` implemented the main fix in the external repo; Codex reviewed via low-token Review Package, escalated to focused target-repo diff because `use_worktree=false` reported no changed_files, added one missing session_id-scan fallback test/fix, rebuilt `release/MiMo-Code-Session-Manager.exe`, accepted the Bridge task, and committed target repo `09f70d03 fix: fallback for cleaned Bridge worktree sessions`. Session Manager docs were then committed as `f3fc5efd docs: add session manager handover`.
- Local Bridge config now includes `C:\Users\86172\Desktop\MiMo Code project\Mimo Code 会话管理` in `%LOCALAPPDATA%\MiMoBridge\config.json` allowedRoots so MiMo can work on that external project. This is local machine state, not a Git-tracked code change and must be recreated on another computer.

- Live viewer enhancement completed and locally verified: `/api/tasks/:id/live` now shows more MiMo-visible runtime text from `part.text`, `state.output`, `state.metadata.output`, and `state.error` in the existing `summary` field. Local paths/session/stdin/token/password values are sanitized; summary budget is raised from 200 to 1000 chars; admin UI live viewer is wider and preserves multiline text. Boundary: this cannot show hidden model chain-of-thought, only text already emitted into MiMo JSONL logs.

- Win11 support is treated as the same Windows x64 release line as Win10 because both are NT 10.x and the current stack is Node/PowerShell/localhost.
- The installer is a MinGW resource-stub EXE that embeds install.ps1 and the portable payload; it should not require system Node on the target computer.
- The installer-created launchers set MIMO_BRIDGE_NODE_PATH, MIMO_BRIDGE_DATA_DIR, and MIMO_BRIDGE_CONFIG explicitly.
- Latest local verification: npm.cmd run package:installer passed; npm.cmd run validate:release -- -SkipPackageBuild passed and wrote artifacts\release-validation.json; artifacts\MiMoBridgeSetup-win10-win11-x64.exe -SelfTest -SelfTestPort 33211 passed; node --test tests/launcher-controller.test.mjs tests/installer-package.test.mjs tests/release-validation.test.mjs passed 20/20.
- Installer EXE supports -SelfTest. This extracts the embedded payload to TEMP, checks required app files, rejects bundled runtime data or MiMo credential files, starts a temporary daemon from bundled node.exe, verifies health and Admin UI HTML, then stops the temporary process without installing anything.
- Documentation cleanup consolidated stale handoff/design snapshots into the active docs listed above and removed obsolete ignored build artifacts that can be regenerated.
- Installer repair after local double-click test: the EXE had installed files but generated installed launcher environment variables across multiple lines, making the app look missing/broken. Fixed `scripts/installer/install.ps1` to write explicit one-line installed launcher variables and fixed the EXE stub to default plain double-click installs to `-Quiet`.
- Current local installed app path: `%LOCALAPPDATA%\MiMoBridgeApp`; current data path: `%LOCALAPPDATA%\MiMoBridge`. After repair install, installed daemon started from bundled `node.exe`, `/api/health` returned ok, MCP status ready, MiMo configured, queue empty.
- Planned P6 is multi-agent dispatch, not provider replacement. Codex should be able to assign work to MiMo and Reasonix concurrently by explicit `agent_id`. Keep existing `mimo_*` tools compatible while adding generic `agent_*` tools. Reasonix TUI is the likely first executable adapter; Reasonix GUI needs a capability probe before any automation commitment.
- P6.0/P6.1 local probe result: `D:\DeepSeek-Reasonix\bin\reasonix.exe` with `REASONIX_HOME=D:\DeepSeek-Reasonix\ReasonixData` reports ready, version `dev`, default model `deepseek`, and providers from redacted `doctor --json`. The current `reasonix-tui` capabilities intentionally have `start_task=false` until the one-shot runner is implemented.
- P6.2 fake-runner and real-smoke proof: `agent_start_task(agent_id="reasonix-tui")` can create a task Worktree, run a Reasonix-compatible one-shot command, write visible output to Bridge JSONL logs, update task status to `review`, and generate a Review Package with changed files. Controlled real smoke task `task_f8b579217015` succeeded with `max_steps=20`, changed only `notes/result.txt`, had `risk_flags: []`, and its temporary Worktree was later discarded so no smoke Worktree remains active.
- P6.2 generic low-token control proof: `agent_get_task` and `agent_wait_task` now reuse the existing bounded `mimo_get_task`/`mimo_wait_task` review path, add optional `agent_id` mismatch protection, and are exposed through MCP plus REST. Verified with root/local-daemon builds and `node --test tests/agent-get-wait-task.test.mjs tests/agent-start-task.test.mjs tests/admin-api.test.mjs tests/stdio-protocol.test.mjs`.
- P6.2 Reasonix session mapping proof: `src/services/reasonix-session-store.ts` scans only configured `REASONIX_HOME\projects` for in-window `.jsonl` session files, skips `.trash`, prefers task/workspace matches, and persists the result as `task.agent_session_path`. Browser API sanitization hides `agent_session_path`; the stored path is for later backend-safe "open Reasonix GUI/session" actions. Verified with `node --test tests/reasonix-session-store.test.mjs tests/agent-start-task.test.mjs`.
- P6.3 first UI slice is implemented locally: the admin UI fetches `/api/agents`, shows agent status in the System page, adds an execution-agent selector to Create Task, routes MiMo tasks to `/api/tasks` and Reasonix tasks to `/api/agent-tasks`, and displays task agent badges in list/detail pages. Reasonix is selectable only when its probe reports `ready` and `capabilities.start_task=true`.
- P6.4 first queue slice is implemented locally: `TaskQueue` can run different agents in parallel only when they use different agents and non-overlapping editable paths in the same workspace; same-agent tasks, unknown-path tasks, and overlapping path tasks still queue. Global queue concurrency is now 2 to allow one MiMo plus one Reasonix task when safe.
- P6.5 Reasonix reply/continue slice is implemented locally: `agent_reply_task` is exposed through STDIO MCP, HTTP MCP, REST `/api/agent-tasks/:id/replies`, and the admin UI reply button. Reasonix TUI replies use the stored `agent_session_path` with `reasonix run --resume <session.jsonl>`; the session path must exist under configured `REASONIX_HOME`. MiMo replies still use `mimo_reply_task` / `/api/tasks/:id/replies`.
- P6.6 Reasonix live/session parser is implemented locally: `src/services/reasonix-event-parser.ts` reads only bounded JSONL tails from stored `agent_session_path`, exposes visible assistant content as primary live messages, folds Reasonix tool calls/results into tool summaries, redacts local paths/session/token/password/API-key patterns, skips user/system records, and intentionally does not display `reasoning_content`. `/api/tasks/:id/live` now merges Bridge runtime logs with Reasonix session events for `reasonix-tui` tasks and can still show session events if the Bridge round log is missing.
- P5.4/P6.10-P6.15 safe agent invocation is implemented. `scripts/mimo-bridge-client.mjs` supports MiMo commands `health`, `start`, `wait`, `reply`, `start-and-wait`, `review`, `recover`, plus generic Agent commands `agent-list`, `agent-start`, `agent-wait`, `agent-reply`, `agent-start-and-wait`, `agent-review`, `agent-cancel`, `agent-finish`, `agent-merge`, `agent-discard`, `agent-delete`, `agent-queue`, `agent-tasks`, `agent-recover`, and read-only `agent-token-status`. Input comes from UTF-8 JSON file or stdin; wait uses MCP SDK with request timeout greater than `timeout_seconds`; CLI output is compact JSON and exits explicitly to avoid stale client processes. `scripts/mimo-bridge-client.ps1` is a thin launcher that locates Node and forwards arguments without constructing JSON.
- TokenBudgetManager now records real MiMo token usage at runner completion by summing `part.tokens` from JSONL events and using MiMo-provided `part.cost` when available. UI copy was updated so a zero value means no completed task has been recorded since daemon start/reset, not "not connected."
- P6.7/P6.17 safe local-open slices are implemented locally: Admin UI task detail has "打开任务文件夹", Reasonix-only "打开会话文件夹", and Reasonix-only "打开 Reasonix GUI" buttons. Browser requests only `POST /api/tasks/:id/open` with fixed actions `task_folder|session_folder|reasonix_gui`; it never sends arbitrary paths or commands. The daemon resolves Worktree -> original allowed workspace fallback for task folders, validates Reasonix session folders under configured `REASONIX_HOME`, and opens Reasonix GUI with the shared `REASONIX_HOME`. Raw local paths are not returned to the browser. Directly opening Reasonix GUI to a specific session remains future work because no stable command/deep link has been confirmed.
- P6.8 Reasonix token/cost extraction is implemented locally: `reasonix-event-parser` extracts explicit `tokens`, `usage`, `token_usage`, `prompt_tokens`, `completion_tokens`, `total_tokens`, and `cost` fields from session JSONL. `ReasonixTuiRunner` records usage into `globalTokenBudget` only when `total_tokens > 0`; no fields means unknown/no record rather than fake estimates.
- P6.9 generic agent lifecycle tools are implemented locally: `agent_cancel_task`, `agent_finish_task`, `agent_merge_task`, `agent_delete_task`, and `agent_queue_status` now exist in STDIO MCP and HTTP MCP. REST also supports `/api/agent-tasks/:id/cancel`, `/finish`, `/worktree`, `DELETE /api/agent-tasks/:id`, and `GET /api/agent-queue`. These wrap existing task lifecycle behavior, add optional `agent_id` mismatch protection, and return the task agent when successful. This makes Reasonix TUI tasks manageable without borrowing `mimo_*` tool names.
- Latest P6.9 verification: root build, local-daemon build, admin-ui build, `node --test tests\agent-lifecycle-task.test.mjs tests\agent-get-wait-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 43/43, and P6 regression `node --test tests\agent-start-task.test.mjs tests\agent-reply-task.test.mjs tests\task-queue.test.mjs tests\reasonix-event-parser.test.mjs tests\live-task-view.test.mjs tests\token-budget.test.mjs` passed 75/75. Windows node-pty `AttachConsole failed` noise appeared during the P6 regression but the test process exited 0.
- P6.18 UI lifecycle parity builds on P6.9: `apps/admin-ui/src/api.ts` now chooses generic lifecycle REST routes for non-MiMo agents, and `apps/admin-ui/src/App.tsx` passes the task's `agent` into cancel/finish/merge/discard/delete handlers. Tests in `tests/admin-ui-lifecycle-api.test.mjs` cover both Reasonix and MiMo route selection.
- P6.9 local daemon deploy check: `launcher.ps1 restart` timed out in the shell but the launcher state PID changed to 25948 and `/api/health` stayed ready. `node scripts\mimo-bridge-client.mjs recover --limit 5 --max-chars 8000` returned `pending_count: 0`. `GET /api/agent-queue?agent_id=reasonix-tui` returned ok with an empty queue, proving the new route is loaded.
- P6.10 safe client Agent commands are implemented locally: scripted callers can now use `agent-start`, `agent-wait`, `agent-review`, lifecycle commands, and `agent-queue` without inline PowerShell/Node JSON. Verification: root/local-daemon/admin-ui builds passed, `node --test tests\mimo-bridge-client.test.mjs` passed 24/24, and focused regression `node --test tests\mimo-bridge-client.test.mjs tests\agent-lifecycle-task.test.mjs tests\agent-get-wait-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 67/67.
- P6.10 live daemon smoke passed: `node scripts\mimo-bridge-client.mjs agent-list` returned ready MiMo and ready Reasonix TUI; `node scripts\mimo-bridge-client.mjs agent-queue --agent-id reasonix-tui` returned ok with empty queue.
- P6.11 safe client reply commands are implemented locally: `reply` posts to MiMo task replies and `agent-reply` posts to generic Agent/Reasonix replies. Both accept `--message` for short text or UTF-8 JSON/stdin for long Chinese and multiline replies. Verification: root/local-daemon/admin-ui builds passed, `node --test tests\mimo-bridge-client.test.mjs` passed 26/26, and focused regression `node --test tests\mimo-bridge-client.test.mjs tests\agent-reply-task.test.mjs tests\agent-lifecycle-task.test.mjs tests\admin-api.test.mjs tests\stdio-protocol.test.mjs` passed 67/67.
- Latest package build after P6.17 succeeded with `npm.cmd run package:installer`. Artifacts: `artifacts\MiMoBridgeSetup-win10-win11-x64.exe` (55,604,224 bytes) and ignored local `artifacts\MiMoBridge-portable-win10-win11-x64.zip` (55,641,531 bytes). `npm.cmd run validate:release -- -SkipPackageBuild` passed and wrote ignored local `artifacts\release-validation.json`; installer self-test exit was 0.
- Safe-delete visibility is implemented and merged via real Codex -> MCP -> MiMo -> Review Package -> focused diff -> merge flow. `/api/tasks` and `/api/tasks/:id` now return `can_delete`, `delete_blockers`, and `delete_label`; the admin UI has a `可安全删除` filter and only shows delete when backend-derived `can_delete` is true.
- Default Chinese display chain: `ReviewPackage` type has optional `objective_zh`, `agent_summary_zh`, and legacy `mimo_summary_zh` fields. When objective or summary contains Chinese characters, the zh field is populated (same content); otherwise zh is omitted. Admin UI `detailToUiTask` prefers generic agent summary fields for summary with legacy MiMo fallback. Task brief `prompt-builder.ts` adds a "语言要求" section requesting Chinese summaries. Codex handoff prompt also requests Chinese. No external translation API or dependency introduced.
- Latest safe-delete verification: `npm.cmd run build`, `cd apps/local-daemon; npm.cmd run build`, `cd apps/admin-ui; npm.cmd run build`, and `node --test tests/admin-api.test.mjs` passed. Daemon was restarted with `launcher.ps1 restart`; `/api/health` was ready and historical accepted/no-Worktree tasks returned `can_delete: true`, `delete_label: "可安全删除"`.
